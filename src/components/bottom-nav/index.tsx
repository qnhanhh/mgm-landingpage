import { BottomButton, BottomContainer, SecondaryButton } from "./styles";

const BottomNav = () => {
  return (
    <BottomContainer>
      <BottomButton>TƯ VẤN KHÓA HỌC</BottomButton>
      <SecondaryButton>ĐĂNG KÝ NGAY</SecondaryButton>
    </BottomContainer>
  );
};

export default BottomNav;
