import { NavButton, NavContainer, NavLogo } from './styles'

const TopNav=()=>{
    return(
        <NavContainer>
            <NavLogo/>
            <NavButton>TƯ VẤN KHÓA HỌC</NavButton>
        </NavContainer>
    )
}

export default TopNav